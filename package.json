{
  "name": "first_tailwind",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "concurrently \"npm run css\" vite",
    "build": "vite build",
    "preview": "vite preview",
    "css": "tailwindcss -i ./src/styles/style.css -o ./src/styles/output.css --watch"
  },
  "devDependencies": {
    "concurrently": "^8.0.1",
    "tailwindcss": "^3.3.0",
    "vite": "^4.2.0"
  }
}
